{
  "manifest_version": 3,
  "name": "Close duplicate tabs",
  "version": "1.0",
  "description": "Closes duplicate tabs, e.g. tabs that are opened though CLI programs, such as vitest-ui",
  "background": {
    "service_worker": "background.js"
  },
  "permissions": [
    "tabs"
  ]
}